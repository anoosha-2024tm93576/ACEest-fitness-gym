apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-canary
spec:
  type: NodePort
  selector:
    app: aceest-fitness
    version: blue
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007