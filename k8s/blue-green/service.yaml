# Service pointing to Blue by default
apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-bg
spec:
  type: NodePort
  selector:
    app: aceest-fitness
    version: blue
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30001
 